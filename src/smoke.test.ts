import { describe, expect, it } from "vitest";

// Placeholder so `npm test` is green from Milestone 1. Real suites arrive with
// the battle engine (Milestone 5) and game data (Milestone 4).
describe("smoke", () => {
  it("runs the test runner", () => {
    expect(1 + 1).toBe(2);
  });
});
